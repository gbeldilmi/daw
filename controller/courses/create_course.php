<?php
  // create_course(name, desc,path)
  session_start();
require_once '../model/sql-request.php';
function create_course($name,$description,$path):bool{
    return create_course($name,$description,$path);
 
} 
